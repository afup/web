<script>
$(document).ready(function () {
    let vatFields = [
        {link: 'apply-vat-5-5', input: 'transaction_montantTva5_5', rate: 0.055},
        {link: 'apply-vat-10', input: 'transaction_montantTva10', rate: 0.1},
        {link: 'apply-vat-20', input: 'transaction_montantTva20', rate: 0.2},
        {link: 'apply-vat-0', input: 'transaction_montantTva0', rate: 0}
    ];
    for (let i = 0; i < vatFields.length; i++) {
        document.getElementById(vatFields[i].link).addEventListener('click', function (event) {
            let value = document.getElementById('transaction_montant').value;
            value = value.replace(',', '.');
            document.getElementById(vatFields[i].input).value = Number.parseFloat(Math.round(parseFloat(value) / (1 + vatFields[i].rate) * 100) / 100).toFixed(2);
            event.preventDefault();
            return false;
        });
    }
});
</script>
