{% extends 'admin/base_with_header.html.twig' %}

{% block content %}
    <h2>Relance des cotisations</h2>
    <table class="ui table striped compact celled">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Société</th>
                <th class="center aligned">Statut mail</th>
                <th>Type relance</th>
                <th>Date relance</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for log in logs %}
            <tr class="{% if loop.index is even %}even{% endif %}">
                <td>{{ log.prenom }} {{ log.nom }}</td>
                <td>{% if log.apmId is not null %}{{ log.raisonSociale }}{% else %}¤{% endif %}</td>
                <td class="center aligned">
                    {% if log.mailSent %}
                        <span class="ui green label">Ok</span>
                    {% else %}
                        <span class="ui red label">Erreur</span>
                    {% endif %}
                </td>
                <td>{{ log.reminderKey }}</td>
                <td>{{ log.reminderDate|date('d/m/Y') }}</td>
                <td class="right aligned">
                    <a data-position="left center"
                       class="compact ui green icon button"
                       {% if log.apmId is not null %}
                           href="{{ url('admin_membership_fee_list', {'memberType': enum('AppBundle\\Association\\MemberType').MemberCompany.value, 'memberId': log.apmId}) }}"
                           data-tooltip="Gérer les cotisations de {{ log.raisonSociale }}"
                       {% else %}
                           href="{{ url('admin_membership_fee_list', {'memberType': enum('AppBundle\\Association\\MemberType').MemberPhysical.value, 'memberId': log.appId}) }}"
                           data-tooltip="Gérer les cotisations de {{ log.prenom }} {{ log.nom }}"
                       {% endif %}
                    >
                        <i class="money bill alternate icon"></i>
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="ui center aligned clearing segment">
        {% if logs|length > limit %}
            <div class="ui right floated segment">
                <a href="{{ url('admin_relances', { page: page + 1 }) }}" class="ui right labeled icon button">
                    <i class="right chevron icon"></i>
                    Page suivante

                </a>
            </div>
        {% endif %}
        {% if page > 1 %}
            <div class="ui left floated segment">
                <a href="{{ url('admin_relances', { page: page - 1 }) }}" class="ui labeled icon button">
                    <i class="left chevron icon"></i>
                    Page précédente
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}
