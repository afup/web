admin_vote:
  path: /vote/{eventSlug}
  defaults: {_controller: AppBundle:Vote:admin}

admin_relances:
  path: /association/relances/{page}
  defaults: {_controller: AppBundle:MemberShip:reminderLog}

admin_company:
  path: /association/membres/entreprises
  defaults: {_controller: AppBundle:MemberShip:admin}

admin_void:
  path: /void
  defaults: {_controller: AppBundle:Legacy:backOffice}

admin_login:
  path: /login
  defaults: {_controller: AppBundle:Admin:login}

admin_logout:
  path: /logout

legacy_inscription:
  path: /register
  defaults: {_controller: AppBundle:Legacy:register}
