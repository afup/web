<div class="ui violet segment">
    {{ form_start(form) }}
        <input type="hidden" name="page" value="forum_sessions" />
        <div class="ui form">
            <div class="inline fields">
                <div class="field">
                    {{ form_label(form.id, 'Choisir un autre événement:') }}
                    {{ form_widget(form.id, {attr: {onchange:'this.name="id"; this.form.submit(); return false;'}}) }}
                </div>
            </div>
        </div>
    {{ form_end(form) }}
</div>
